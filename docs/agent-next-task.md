# ReviewRadar Agent Handoff

Updated: 2026-08-29 by Codex after the independently approved PR-3H candidate-
quarantine correction. This file was regenerated from current evidence. The
current approved base is the self-contained PR-3H closeout commit containing
this file; resolve its exact full SHA with `git rev-parse HEAD`. Its expected
parent is `ac53c10e707b67945f7df4f4cbda129c7c6ef69d`.

## Current state

ReviewRadar is **not production-ready**. The staged Terra path remains default-
off, undeployed, and without a completed live shopper result.

PR-3G spent exactly one frozen `shop vac` lifecycle at clean PR-3F commit
`ac53c10e707b67945f7df4f4cbda129c7c6ef69d`. The provider reached terminal
`completed`, but strict local research validation stopped as
`research_candidate_invalid / candidate_sources /
candidate_source_identity_unproven` before page collection, Shopping,
deterministic verification, presentation, or rendering.

The 75.501-second first-terminal run used one OpenAI create, 31 retrieves, six
hosted searches, one safety cancel, 56,876 input tokens, zero cached input, and
7,759 output tokens. Frozen approval-envelope cost was `$0.318575`; frozen-
conservative cost was `$0.354123`; the informational current estimate was
`$0.266860`. No retry, replacement, fallback, organic/SearchAPI request,
product-data request, second case, post-stop call, public result, flag change,
or deployment occurred.

The only PR-3G artifact is untracked 26,300-byte
`tests/fixtures/review-radar-live/oai-t10-phase-d-ac53c10/attempt.json`, SHA-256
`2E3296650224BF28EEE56166860A9B82C22EC7ED6B93C374594F2869FDE0C1CE`.
Independent strict read-only audit returned `VERIFIED`, no findings, confidence
0.99. It authenticates the exact commit/case/evidence-v4 boundary, terminal
sequence, counters, usage, cost, and privacy. It contains no raw/provider/
candidate/product/source identity, prompt, body, header, credential, key,
secret, or token. Its exact rejection distribution and survivor count are
unknown. The directory is immutable and spent.

A preliminary CLI form used separated approval tokens and was rejected by the
local parser before counters, directory creation, network, artifact, or spend.
The one actual lifecycle used required `--name=value` tokens and was not
retried. The completed response below the output cap refutes the token-cap
hypothesis; it does not authorize a prompt-only patch or weaker identity.

PR-3H corrects the reproduced all-or-nothing handling. The provider still must
submit 8–15 fully schema-valid candidates, each with exactly two fetch-distinct
exact response-owned sources. After parsing, a candidate survives only when at
least one of its two exact title/URL records passes the unchanged shared asset-
identity verifier. Rejected candidates and their URLs never reach page fetch,
Shopping, evidence, or shopper output. Survivors retain discovery order and
receive contiguous server-owned candidate and fact IDs. Zero survivors retain
the exact bounded failure triple.

The closed server-only filter diagnostic contains submitted, accepted, and
rejected counts plus five reachable reason families: missing title, brand not
in title, model not in title, conflicting model, and wrong product type. The
verifier contributes one or two distinct families for each rejected candidate.
Exact-key route sanitization requires safe integers, arithmetic conservation,
each family no greater than the rejected count, an aggregate reason total from
the rejected count through twice that count, equality between completed counts
and the actual survivor slate, and the exact zero-survivor failure context.
Malformed, private, under-attributed, over-attributed, or context-false
evidence is omitted. Impossible post-parse target states fail closed.

Contract v8 and runtime v7 roll old jobs closed; future sanitized Phase D
evidence is v5. Research schema v5 and prompt v6 remain unchanged. No fetched-
source, complete-product relationship, commerce, price, asset, requirement,
evidence, eligibility, ranking, public-response, flag, UI, network-ceiling, or
deployment rule changed. PR-3H made no external request.

## PR-3G and PR-3H proof and limits

| Check | Result |
| --- | --- |
| PR-3G exact artifact audit | `VERIFIED`; no findings; confidence 0.99 |
| PR-3H fail-first | 58 passed / exactly 12 intended failures |
| Corrected focused wall | 126/126 |
| Complete deterministic suite | 1,495/1,495 across 214 suites |
| Final controller | `agent-loop-2026-08-29T12-07-42-962Z`; passed |
| Five deterministic batch partitions | exact reconciliation passed |
| Tracked offline benchmark | 10/10 cases; 29/29 invariants |
| Credential-neutral E2E | 17/17 |
| Typecheck and production build | pass |
| Legacy eval and fixed ranking comparison | pass; no red flags |
| Phase D dry run | pass; zero network |
| Lint | 0 errors / 3 pre-existing warnings |
| Diff and generated-file state | pass; `next-env.d.ts` clean |
| Independent PR-3H review | `APPROVED`; no findings; confidence 0.97 |
| PR-3H live/provider/product-data calls | none |

Independent review found context-unbound success/failure counts, a missing
upper bound implied by two source decisions per rejected candidate, and two
unreachable proposed reason buckets. The corrected implementation binds counts
to their surrounding record, enforces one-to-two reasons, retains only five
reachable families, and fails impossible states closed. The reviewer personally
passed 89/89, non-incremental typecheck, diff checks, and exact tracked-state
reauthentication.

Two overbroad repository searches traversed live-fixture paths. One matched
only already-known PR-3G terminal outcome keys; one later legacy-fixture search
matched only fixed rejection-reason tokens. No raw/private field, product/source
identity, body, prompt, or credential was displayed. All subsequent repository
searches excluded `tests/fixtures/review-radar-live/**`. No fixture was edited,
staged, retried, or reused.

This proves only the generalized offline correction. Candidate quarantine can
reduce recall. It does not establish live survivor yield, lifecycle
feasibility, recommendation quality, latency, cost, stability, or market
coverage.

## Objective and decision frame for the next phase

The earliest dependency remains PR-013 staged lifecycle feasibility. PR-4
active-path accuracy/stability measurement still requires one complete safe
staged shopper lifecycle.

Stronger alternatives were evaluated:

- Reading, editing, or retrying PR-3G is prohibited; its terminal evidence and
  hash are immutable.
- Raising the output cap is contradicted by PR-3G's provider-completed response
  below the cap.
- A prompt-only patch leaves schema-valid all-or-nothing consumer behavior
  intact and cannot be verified from the private artifact.
- Weakening title/model/type, relationship, commerce, or asset identity would
  manufacture ambiguous cards rather than preserve safe survivors.
- Lowering the provider's 8-candidate minimum would change the research
  contract and hide provider adherence; PR-3H instead filters only after strict
  validation.
- A broad live matrix remains premature until one staged lifecycle succeeds.

The strongest next step is one PR-3I measurement of the unchanged frozen `shop
vac` case at the clean PR-3H closeout commit. This is not a PR-3G retry: it uses
a new commit-derived directory and tests contract v8/runtime v7/evidence v5. A
stop selects only the next generalized zero-live reproduction target. A safe
shopper result unlocks PR-4.

**Recommended reasoning level:** High for live trust, privacy, cost, and
terminal-evidence adjudication; Medium for the bounded mechanical invocation.

## Current approved phase: PR-3I one post-quarantine lifecycle measurement

Taylor's 2026-08-29 production-readiness mandate authorizes this one bounded
low-parallelism measurement after a clean PR-3H commit. It does not authorize a
retry, replacement, broader matrix, flag promotion, deployment, or publication.

1. Authenticate this repository, branch `main`, exact PR-3H HEAD/parent, clean
   tracked and index state, clean `next-env.d.ts`, committed default-off flags,
   and unchanged user-owned untracked artifacts.
2. Run the focused Phase D runner tests and inspect the zero-network dry run
   from the exact commit. Check required server credentials only for in-memory
   presence and safe shape; never print, copy, hash, or stage their values.
3. Confirm the new commit-derived output directory does not exist. Do not read,
   edit, retry, stage, reuse, or add files to any spent Phase D directory.
4. Run exactly one lifecycle command using only `--name=value` approval tokens:

   ```powershell
   $pr3hCommit = (git rev-parse HEAD).Trim()
   node scripts/run-oai-t10-phase-d.mjs --execute --approved-commit=$pr3hCommit --approved-openai-creates=2 --approved-hosted-searches=10 --approved-openai-retrieves=60 --approved-safety-cancels=1 --approved-serper-shopping-attempts=15 --approved-source-page-fetches=30 --approved-source-page-http-attempts=90 --approved-dollar-ceiling=3
   ```
5. Stop at the first terminal route/result state. Do not retry, replace, add a
   case, continue downstream after a stop, fall back, use organic/SearchAPI,
   promote a flag, or deploy. The single invocation spends PR-3I regardless of
   outcome.
6. Validate exact commit/case/evidence-v5 schema, counters, usage, cost, hashes,
   privacy/credential exclusions, terminal sequence, filter-count conservation
   and context binding, downstream first-loss conservation if reached, and
   public-response reconciliation. Treat the artifact as immutable spent
   evidence immediately.
7. Obtain independent read-only audit of the exact artifact and clean tracked
   state. Terminal audit verdict must be `VERIFIED`; a failed lifecycle remains
   evidence and never authorizes a second invocation.
8. A smaller slate is not success. If the route stops, select an evidence-
   supported target for a separate generalized zero-live reproduction. If it
   returns a safe shopper result, begin PR-4 planning against the tracked
   benchmark and reviewed current-market truth.
9. Update the living plan and authoritative records. Keep every live artifact
   untracked. Commit only phase-owned documentation or a separately verified
   deterministic correction; never stage live evidence.

## Approval, cost, and flag state

Committed defaults remain:

- `REVIEW_RADAR_STAGED_TERRA=off`
- `NEXT_PUBLIC_REVIEW_RADAR_STAGED_TERRA=false`
- `REVIEW_RADAR_DIRECT_TERRA=off`
- `NEXT_PUBLIC_REVIEW_RADAR_DIRECT_TERRA=false`
- `REVIEW_RADAR_CONSTRAINT_ALLOCATION=off`
- staged research/presentation model: `gpt-5.6-terra`

The ignored developer `.env.local` is user-owned and locally enables Direct
Terra. Never edit, stage, or print it. The frozen hard ceiling is `$3`; current
pricing is informational and cannot widen that wall.

## Outstanding readiness debts

- PR-006: current leader recall, final-set stability, hard-requirement truth,
  price coverage, latency distribution, calls, and cost are not benchmarked.
- PR-007 / RR-091: same-page related-product price binding needs exact-offer
  proof before an experimental path can be promoted.
- PR-008 / RR-092: editorial Product markup can overstate tested-model
  identity/image authority.
- PR-013: staged lifecycle feasibility remains blocked; no live attempt has
  reached presentation/rendering.
- PR-019: candidate quarantine is closed offline, but live survivor yield,
  recall effect, and lifecycle effect are unmeasured.
- Broader cache/concurrency, security, accessibility, mobile UX, production
  configuration, rollback, dependency, and observability gates remain planned
  in `docs/production-readiness-master-plan.md`.

## Hard boundaries

- Never read, retry, edit, stage, reuse, or add to any spent Phase D attempt or
  directory, including `oai-t10-phase-d-ac53c10`,
  `oai-t10-phase-d-bf7e37b`, `oai-t10-phase-d-a743426`,
  `oai-t10-phase-d-43857e0`, `oai-t10-phase-d-ec528d7`,
  `oai-t10-phase-d-a15d935`, `oai-t10-phase-d-140d465`,
  `oai-t10-phase-d-56a5137`, `oai-t10-phase-d-b01c335`, and
  `oai-t10-phase-d-06fa55f`. Exclude the entire live-fixture tree from broad
  repository searches.
- PR-3I permits exactly one new frozen invocation at the clean PR-3H commit.
  No retry, replacement, second case, broader live QA, or approval reuse.
- Do not infer PR-3G candidate/source mappings or rejection distribution from
  the bounded counts. Reproduce generalized paths offline before naming causes.
- Do not restore model-authored `product_name`, one-source cardinality, raw lead
  URLs, response-global indexes, canonical ownership matching, cross-variant
  title borrowing, same-page fragment multiplicity, whole-response rejection
  for one ungrounded candidate, or cross-candidate evidence borrowing.
- Do not lower the strict provider candidate count, accept an ungrounded
  candidate, or let diagnostic counts authorize trust decisions.
- Do not weaken brand, model, product type, relationship, requirement,
  availability, price, source/page, commerce, asset, redirect, private-network,
  diagnostic, or public-response boundaries.
- Never stage `.env.local`, `.claude/`, `.rr_baseline*`, `fable-transfer-kit/`,
  ignored worker results, historical live fixtures, or any Phase D fixture.
  Never use `git add -A`.
- No deployment, production change, push, publication, destructive action, or
  external scope expansion is authorized.

## Retrieval map

| Need | Retrieve |
| --- | --- |
| Living defects, sequence, exit criteria, PR-3I scope | `docs/production-readiness-master-plan.md` |
| PR-3G artifact and PR-3H canonical verification | latest 2026-08-29 entry in `docs/qa-loop-results.md` |
| Staged architecture and phase sequence | OAI-T10 in `docs/forward-roadmap.md` |
| Candidate quarantine and diagnostic | `lib/stagedTerraContract.ts`; `lib/stagedTerraRecommendationRoute.ts` |
| Staged runtime and job rollover | `lib/stagedTerraRuntime.ts`; `tests/stagedTerraIntegration.test.mjs` |
| Phase D runner and ceilings | `scripts/run-oai-t10-phase-d.mjs`; `scripts/oai-t10-phase-d.mjs` |
| Offline benchmark integrity | `tests/fixtures/qa-benchmark-matrix-v1.json`; `scripts/qa-benchmark.mjs`; `tests/qaBenchmark.test.mjs` |
