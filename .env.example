# Required server-only OpenAI key for product research and ranking.
OPENAI_API_KEY=

# Optional model override for the same ranking backend.
OPENAI_RESEARCH_MODEL=gpt-5.5

# Optional server-only Serper.dev key for product images.
# Without it, ranked products still return with their names and source links.
SERPER_API_KEY=
